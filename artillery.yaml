config:
  target: http://localhost:5000
  phases:
    - duration: 30
      arrivalRate: 10
    - duration: 30
      arrivalRate: 15
    - duration: 30
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "/"
